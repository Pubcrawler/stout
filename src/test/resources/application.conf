postgres {
  dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
  properties = {
    driver = "org.postgresql.Driver"
    url = "postgres://postgres:password@127.0.0.1:7000/postgres"
  }
  numThreads = 10
}
